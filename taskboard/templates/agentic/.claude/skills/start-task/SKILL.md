---
name: start-task
description: >
   Взять задачу в работу: перевести в рабочий статус проекта, обновить board.md,
   собрать контекст задачи и приступать к реализации.
   Используй когда пользователь говорит: "начни задачу", "стартуй задачу",
   "приступай к задаче", "возьми в работу", "берись за TASK-NNN", "start task".
argument-hint: "<TASK-NNN> [дополнительный контекст]"
---

Возьми задачу в работу: статус → доска → контекст → реализация.

## Аргументы

Что скилл принимает — в `argument-hint` его заголовка. Аргументы **позиционные**:
первый имеет форму, остальное разбирается по описанию шагов.

**`--info`** (а также `--help`, `-h`, `?`) — покажи сигнатуру и ничего не выполняй:
что скилл делает, что принимает, два примера вызова, чего он делать не станет.

**Первый токен похож на флаг** (`--что-угодно`, `-x`, `?`), но это не `--info`, —
он не содержимое и не идентификатор. Покажи ту же сигнатуру и скажи, что такого
аргумента нет. **Не угадывай смысл**: именно так заводится задача с названием
`--help` и выпускается не та версия.

Первый токен подошёл — дальше всё как описано ниже, включая свободный текст.

## Шаг 0. Узнать пайплайн проекта

Имена статусов настраиваются per-project — **не подставляй их по памяти**:

```
py tasks/set_status.py --list            (Windows)
python3 tasks/set_status.py --list
```

Лаунчера `py` может не быть (Python из Microsoft Store его не ставит) —
тогда та же команда через `python`. Подробнее — в правилах проекта,
раздел «Чем запускать скрипты».

Из ответа нужны `actions.start` (куда переводить задачу) и `actions.pick`
(раздел, откуда берут работу). Скрипта нет — работай по разделам `tasks/board.md`.

## Шаг 1. Определить задачу

Если первый аргумент передан (TASK-NNN) — использовать его.

Иначе — найди задачу в текущем контексте разговора (последняя упоминавшаяся TASK-NNN).

Если задача всё ещё неясна — **спроси очередь у скрипта**, не полагайся на
прочитанное в начале сессии: доска правится через UI и другими агентами, и
сохранённый в контексте снимок мог устареть.

```
py tasks/set_status.py --queue             (Windows)
python3 tasks/set_status.py --queue
```

Ответ — раздел `actions.pick` прямо сейчас: `tasks` в порядке следования
(первая — приоритетная) и `total` — сколько всего в очереди. Скрипта нет
(старый проект) — перечитай `tasks/board.md` заново.

У каждой записи есть `stalled`: задача ждёт другую (`blocked_by`) или стоит на
паузе (`paused`). **Стоящие пропускай** — предлагай первую свободную.

Затем используй инструмент вопроса
(Claude Code: `AskUserQuestion`; opencode: `question`):
- Q: "Какую задачу начать?"
- Предложи **свободные** задачи из раздела `actions.pick` по порядку (верхняя —
  приоритетная); если очередь пуста — из раздела `actions.create`
- Пропустил стоящие — скажи об этом одной строкой: какие и чего ждут

Реши файл через `glob`: `tasks/TASK-NNN-*.md`.

## Шаг 1.2. Проверить, не стоит ли задача

Задачу могли назвать прямо (аргументом или в разговоре) — тогда очередь её не
фильтровала. Спроси срез простоя:

```
py tasks/set_status.py --stalled             (Windows)
python3 tasks/set_status.py --stalled
```

Задача в этом срезе — **не бери её в работу**. У пользователя на доске есть
подтверждение переноса, у тебя его нет:

- назови причину (кого ждёт или текст паузы) и предложи верхнюю свободную из очереди;
- блокер уже завершён — предложи снять пометку
  (`py tasks/set_status.py TASK-NNN --unblock TASK-MMM`) и только потом стартовать;
- пользователь настаивает — стартуй, но статус меняй с `--force` (Шаг 5):
  без него скрипт откажет.

## Шаг 1.1. Дополнительный контекст задачи

Если передан второй аргумент (дополнительный контекст задачи):
- Запомни его как дополнение к описанию задачи.
- Он может содержать: уточнения по реализации, ссылки на MR/коммиты/тикеты, конкретные архитектурные решения, требования к совместимости, ограничения, приоритеты и т.д.
- Учитывай этот контекст при формировании плана (Шаг 7) и реализации.

Выведи пользователю подтверждение: «Дополнительный контекст принят: <текст>».

## Шаг 2. Прочитать текущее состояние

Прочитай файл задачи `tasks/TASK-NNN-*.md` и `tasks/board.md`.

Если задача уже прошла `actions.start` по пайплайну (её статус стоит правее рабочего —
см. `--targets TASK-NNN`, там он попадёт в `backward`) — предупреди пользователя и
спроси, действительно ли стартовать/продолжить.

## Шаг 3. Понять задачу

Извлеки из файла: `title`, `epic`, описание, критерии приёмки.
Прочитай `tasks/epics.md` и определи **имя эпика** (если эпик указан).
Учти **дополнительный контекст задачи** (если был передан вторым аргументом) — он дополняет/уточняет описание.

Сформулируй для себя: **что** сделать, **зачем**, какие ограничения/критерии.

### Привести текст задачи в читаемый вид

Задачу мог завести человек через форму — тогда описание почти наверняка идёт
сплошным текстом без разметки. Оформи его по правилам проекта (абзацы, списки,
акценты, подзаголовки) **до** начала работы: дальше эту задачу будут читать все,
включая тебя в следующей сессии.

- **Меняй только оформление.** Формулировки автора сохраняй дословно: ничего не
  дописывай, не выбрасывай и не «улучшай» по смыслу.
- Описание уже оформлено — не трогай.
- Текст противоречив или непонятен — не чини молча, **спроси пользователя**.
- То же касается «Критериев приёмки».

Скажи пользователю одной строкой, что описание переформатировано.

<!-- vault -->
## Шаг 4. Собрать знания из Knowledge Vault

Это ключевая ценность скилла — не начинать с нуля, а переиспользовать известное.

1. Прочитай `vault/SYS/taxonomy.md` — доступные домены и теги.
2. Определи **кандидатов-доменов** по:
   - **имени эпика** из `tasks/epics.md` (оно часто прямо указывает на домен волта);
   - **ключевым словам** из title/description.
3. Для каждого кандидата прочитай заметки в `vault/<domain>/` — **сначала `*-business`, затем `*-code`**.
4. Если домен неочевиден — перечисли файлы волта по явному пути (`ls vault/*/`) и открой подходящие по именам. Обычный поиск по проекту волт **не видит**: папка в `.gitignore`, и glob/grep её пропускают. Нужен именно текстовый поиск — только с отключением ignore-фильтра: `rg --no-ignore "<ключевое слово>" vault/`.
5. Зафиксируй коротко: какие бизнес-правила, паттерны, контракты, ключевые файлы уже известны.
6. Если релевантных заметок нет — явно отметь: «в волте нет релевантных заметок».
<!-- /vault -->

## Шаг 5. Перевести в рабочий статус

Целевой статус — `actions.start` из шага 0 (в большинстве проектов `development`).

Статус меняется **одним вызовом скрипта** — он правит и frontmatter, и board.md,
переносит строку в раздел статуса первой, обновляет хвост `· АГЕНТ · дата`
и возвращает заглушку `_(нет)_` в опустевший раздел:

```
py tasks/set_status.py TASK-NNN <actions.start> --agent "<АГЕНТ (модель)>"     (Windows)
python3 tasks/set_status.py TASK-NNN <actions.start> --agent "<АГЕНТ (модель)>"
```

Задача стоит, а пользователь всё равно велел её взять — добавь `--force`: без
него скрипт откажется и напомнит, чего задача ждёт.

Если скрипта в проекте нет — правь вручную оба места: `status:` во frontmatter и
строку задачи в разделе рабочего статуса первой строкой, с хвостом
`· <АГЕНТ (модель)> · YYYY-MM-DD`; в опустевшем разделе оставь `_(нет)_`.
Никогда не оставляй задачу в двух разделах одновременно.

Затем добавь стартовую заметку агента — **тем же скриптом**, а не правкой файла:

```
py tasks/set_status.py TASK-NNN --note "старт: план — вынести парсер, тест на пустой board" --agent "<своя модель>"
```

Скрипт возьмёт время из системы и поставит строку в конец секции «Заметки агента»:

```
- **2026-03-02 23:59** · Claude Sonnet 4.6 · старт: план — вынести парсер, тест на пустой board
```

Модель — **своя**, из текущей сессии, а не скопированная из соседней строки.
Скрипта в проекте нет — пиши строку руками в том же формате, **отдельной строкой
списка** (иначе markdown склеит её с соседней в один нечитаемый абзац), время бери
из системы (`date +%H:%M`), а саму строку добавляй в конец секции.

## Шаг 6. Изучить кодовую базу

- Найди `grep`/`glob` существующие похожие реализации, сервисы, контракты, репозитории.
- Сверься с правилами проекта (`AGENTS.md` / `CLAUDE.md`): архитектура затрагиваемых слоёв, workflow после изменений.
- **Переиспользуй** существующий код — не дублируй.

## Шаг 7. Представить план и приступить к реализации

Кратко покажи пользователю: суть задачи + дополнительный контекст (если был) + находки в коде + план реализации.

Затем реализуй, следуя правилам проекта:
- **TDD** там, где есть тестируемая логика (RED → GREEN → все тесты зелёные). Если автотесты что-то не покрывают — ручная проверка через доступные интерфейсы (HTTP, консоль, UI).
- **Деструктивные операции с данными** (БД, файлы, продакшн-контуры) — только с подтверждения пользователя.
- **Не коммить** без явного запроса пользователя.
- **Если план расходится с обсуждённым или требует архитектурных решений** — остановись и согласуй.
- В ходе работы добавляй **компактные заметки агента** — командой `--note` **по ходу
  работы**, а не пачкой в конце: время в строке означает момент события (формат и
  лимит — см. правила проекта).

## Шаг 8. Проверка после реализации

- Запусти проверки проекта (линтеры, статический анализ, тесты — согласно правилам проекта).
- Ручная проверка через HTTP/консоль/UI, если применимо.

## Шаг 9. Отдать задачу на проверку

Проверки прошли — работа кончилась, и задача уходит из рабочего статуса. Это
делается скиллом **`handoff-task`**: он закроет чекбоксы сделанного, сохранит
добытое знание, переведёт статус и скажет человеку, что именно проверять.

**Вызови его сам, не дожидаясь просьбы.** Пользователь не знает ни про статусы,
ни про то, что знание пора записывать, — он ждёт, что ему скажут, куда смотреть.
И не двигай статус в обход: перевод — шаг того скилла, а всё остальное, что он
делает, в обход просто не случится.

## Важно

- Скилл = подготовка контекста + старт.
- **Работа кончилась — зови `handoff-task`** (Шаг 9), а не меняй статус вручную.
<!-- vault -->
- Не пропускай сбор знаний из волта (Шаг 4) — ради этого скилл и нужен.
<!-- /vault -->
- `board.md` и файл задачи — единый источник правды по статусу; держи их синхронно.
- **Заголовки секций файла задачи — его структура**: не сноси, не переименовывай и не
  переставляй их, «История коммитов» остаётся последней. Скрипт предупредит (`[!] …`),
  если структура разъехалась или заметки оказались не в хронологии.
- Очередь берётся в момент старта, а не из памяти сессии: перед тем как называть
  задачи пользователю, спроси `py tasks/set_status.py --queue`.
- Не коммить, не выполняй деструктивных операций без подтверждения, сверяйся с правилами проекта.
- **При каждом коммите** по задаче: строка в секцию **«История коммитов»** файла задачи — формат в правилах проекта, § «Правила работы с задачей». Пустая секция не пропадёт незамеченной: скрипт скажет о ней при уходе с этапа работы.
