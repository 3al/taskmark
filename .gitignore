# Окружение и зависимости
.venv/
taskboard/.venv/
taskboard/frontend/node_modules/
__pycache__/
*.pyc

# Временное/staging
tmp/
taskboard/tmp/
# Мусор крэш-хендлера Sentry от сторонних приложений,
# запущенных с рабочим каталогом в корне репозитория
.sentry-native/

# Настройки IDE
.idea/
.vscode/

# Задачи и волт самого taskboard'а.
# tasks/ здесь — отдельный репозиторий со своим remote, поэтому поставляемый
# tasks/.gitignore («*») к ней неприменим: он спрятал бы её файлы от неё самой.
/tasks/
/vault/
